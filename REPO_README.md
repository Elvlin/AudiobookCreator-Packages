# AudiobookCreator-Packages

Release assets and manifests for the Audiobook Creator downloader setup.

This repository is for packaged installer components, not the main app source code.

It hosts:

- app core package
- runtime packages
- Python backend packages
- tool packages
- model packages
- `packages.json`
- `SHA256SUMS.txt`

Main source repository:

- `AudiobookCreator`
